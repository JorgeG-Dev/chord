use std::path::PathBuf;

use anyhow::Result;

pub fn add(name: String, remote: String, revision: String, location: PathBuf) -> Result<()> {
    Ok(())
}

pub fn remove(name: String) -> Result<()> {
    Ok(())
}
